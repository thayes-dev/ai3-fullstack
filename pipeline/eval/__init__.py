"""
pipeline.eval -- Evaluation layer for the RAG pipeline.

This package contains:
    golden_set  -- The curated test set used for all evaluations
    tasks       -- Task closures that run a pipeline end-to-end (retrieval + generation)
    evaluators  -- Pass/fail functions that grade a task's output

Students build evaluators live in Session 1.2; the rest is provided.
"""
